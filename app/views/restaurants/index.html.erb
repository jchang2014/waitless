<div class="restaurant-results">
    <h1>Match Results</h1>

    <section class="results-list-wrapper">

    <% if @results %>
  	 <%= render :partial => "restaurants/result", collection: @results, as: :result %>
    <% else %>
	     Sorry, your search yielded no results
    <% end %>
    </section>

  </div>

